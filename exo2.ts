/**
 * Small function to calculate two numbers.
 * @param x - number 1
 * @param y - number 2
 * @returns - Number representing the sum of the two numbers.
 */

export function calc(x:number, y:number):number {
    return x + y;
}
console.log("6 + 4 = " + calc(6, 4));